##########################################################
# THE FOLLOWING LINES IS USED BY docker-build
##########################################################
requires:
  archlinux:
    # Useful URL: https://git.archlinux.org/svntogit/community.git/tree/marco/trunk/PKGBUILD

  debian:
    # Useful URL: https://github.com/mate-desktop/debian-packages
#    - autoconf-archive
#    - autopoint
    - clang
    - clang-tools
    - cppcheck
    - gcc
    - git
    - build-essential
    - docbook-xml
      gettext
    - gobject-introspection
    - gtk-doc-tools
    - libgirepository1.0-dev
    - libglib2.0-dev
    - libglib2.0-doc
    - libgtk-3-dev
    - libgtk-3-doc
    - libstartup-notification0-dev
    - libx11-dev
    - libxext-dev
    - libxres-dev
    - meson
    - ninja-build

  fedora:
    # Useful URL: https://src.fedoraproject.org/cgit/rpms/libwnck.git
#    - autoconf-archive
    - clang-analyzer
    - clang
    - cppcheck
    - cppcheck-htmlreport
    - gcc
    - git
    - gettext
    - glib2-devel
    - gobject-introspection-devel
    - gtk3-devel
    - gtk-doc
    - libXres-devel
    - meson
    - ninja-build
    - pango-devel
    - startup-notification-devel

  ubuntu:

variables:
  - 'CHECKERS="
    -enable-checker deadcode.DeadStores
    -enable-checker alpha.deadcode.UnreachableCode
    -enable-checker alpha.core.CastSize
    -enable-checker alpha.core.CastToStruct
    -enable-checker alpha.core.IdenticalExpr
    -enable-checker alpha.core.SizeofPtr
    -enable-checker alpha.security.ArrayBoundV2
    -enable-checker alpha.security.MallocOverflow
    -enable-checker alpha.security.ReturnPtrRange
    -enable-checker alpha.unix.SimpleStream
    -enable-checker alpha.unix.cstring.BufferOverlap
    -enable-checker alpha.unix.cstring.NotNullTerminated
    -enable-checker alpha.unix.cstring.OutOfBounds
    -enable-checker alpha.core.FixedAddr
    -enable-checker security.insecureAPI.strcpy"'

before_scripts:

build_scripts:
  - if [ ${DISTRO_NAME} == "debian" ];then
  -     export CFLAGS+=" -Wsign-compare"
  -     cppcheck --enable=warning,style,performance,portability,information,missingInclude --force -D'PACKAGE' -D'WITH_VERBOSE_MODE' .
  - fi

#  - NOCONFIGURE=1 ./autogen.sh
#  - scan-build $CHECKERS ./configure --enable-compile-warnings=maximum
#  - if [ $CPU_COUNT -gt 1 ]; then
#  -     if [ ${DISTRO_NAME} == "debian" ];then
#  -         scan-build $CHECKERS --keep-cc --use-cc=clang --use-c++=clang++ -o html-report make -j $CPU_COUNT
#  -         make clean
#  -     fi
#  -     scan-build $CHECKERS --keep-cc -o html-report make -j $CPU_COUNT
#  - else
#  -     if [ ${DISTRO_NAME} == "debian" ];then
#  -         scan-build $CHECKERS --keep-cc --use-cc=clang --use-c++=clang++ -o html-report make
#  -         make clean
#  -     fi
#  -     scan-build $CHECKERS --keep-cc -o html-report make
#  - fi

after_scripts:
#  - if [ ${DISTRO_NAME} == "fedora" ];then
#  -   cppcheck --xml --output-file=cppcheck.xml --enable=warning,style,performance,portability,information,missingInclude --force -D'PACKAGE' -D'WITH_VERBOSE_MODE' .
#  -   cppcheck-htmlreport --title=${REPO_NAME} --file=cppcheck.xml --report-dir=cppcheck-htmlreport
#  -   ./gen-index-other-dns -l 20 -i https://github.com/${OWNER_NAME}/mate-icon-theme/raw/master/mate/16x16/apps/preferences-system-windows.png
#  - fi
  - ls -la
# meson dist gives "Dist currently only works with Git or Mercurial repos"
  - meson setup build -Dprefix=/usr
  - meson dist -C build

releases:
  draft: false
  prerelease: false
  checksum: true
  file_glob: true
  files: libmatewnck-*.tar.xz
  github_release:
    tags: true
    overwrite: true
    base_version: 1.20.0
    notify_servers:
      - https://release.mate-desktop.org/release
